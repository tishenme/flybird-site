# Mac

!!! tip
    Mac 手成本高 但是带来的体验是非常棒的 除非找不到你想要的软件

## 系统工具

- ### 运行环境

{{ pd_read_csv('notes/soft/data/softwares.csv').query('system == "mac"').query('catalog == "运行环境"').drop('system', axis=1).drop('catalog', axis=1).to_markdown(index=False, tablefmt="github") }}

- ### 系统优化

{{ pd_read_csv('notes/soft/data/softwares.csv').query('system == "mac"').query('catalog == "系统优化"').drop('system', axis=1).drop('catalog', axis=1).to_markdown(index=False, tablefmt="github") }}

- ### 安全防护

{{ pd_read_csv('notes/soft/data/softwares.csv').query('system == "mac"').query('catalog == "安全防护"').drop('system', axis=1).drop('catalog', axis=1).to_markdown(index=False, tablefmt="github") }}

- ### 数据恢复

{{ pd_read_csv('notes/soft/data/softwares.csv').query('system == "mac"').query('catalog == "数据恢复"').drop('system', axis=1).drop('catalog', axis=1).to_markdown(index=False, tablefmt="github") }}

- ### 硬件工具

{{ pd_read_csv('notes/soft/data/softwares.csv').query('system == "mac"').query('catalog == "硬件工具"').drop('system', axis=1).drop('catalog', axis=1).to_markdown(index=False, tablefmt="github") }}

- ### 窗口控制

{{ pd_read_csv('notes/soft/data/softwares.csv').query('system == "mac"').query('catalog == "窗口控制"').drop('system', axis=1).drop('catalog', axis=1).to_markdown(index=False, tablefmt="github") }}

- ### 文件管理

{{ pd_read_csv('notes/soft/data/softwares.csv').query('system == "mac"').query('catalog == "文件管理"').drop('system', axis=1).drop('catalog', axis=1).to_markdown(index=False, tablefmt="github") }}
