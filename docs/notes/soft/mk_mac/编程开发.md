# Mac

!!! tip
    Mac 手成本高 但是带来的体验是非常棒的 除非找不到你想要的软件

## 编程开发

- ### 虚拟机

{{ pd_read_csv('notes/soft/data/softwares.csv').query('system == "mac"').query('catalog == "虚拟机"').drop('system', axis=1).drop('catalog', axis=1).to_markdown(index=False, tablefmt="github") }}

- ### 版本控制

{{ pd_read_csv('notes/soft/data/softwares.csv').query('system == "mac"').query('catalog == "版本控制"').drop('system', axis=1).drop('catalog', axis=1).to_markdown(index=False, tablefmt="github") }}

- ### 编程工具

{{ pd_read_csv('notes/soft/data/softwares.csv').query('system == "mac"').query('catalog == "编程工具"').drop('system', axis=1).drop('catalog', axis=1).to_markdown(index=False, tablefmt="github") }}

- ### 数据库工具

{{ pd_read_csv('notes/soft/data/softwares.csv').query('system == "mac"').query('catalog == "数据库工具"').drop('system', axis=1).drop('catalog', axis=1).to_markdown(index=False, tablefmt="github") }}

- ### 内网穿透

{{ pd_read_csv('notes/soft/data/softwares.csv').query('system == "mac"').query('catalog == "内网穿透"').drop('system', axis=1).drop('catalog', axis=1).to_markdown(index=False, tablefmt="github") }}

- ### 工具集锦

{{ pd_read_csv('notes/soft/data/softwares.csv').query('system == "mac"').query('catalog == "工具集锦"').drop('system', axis=1).drop('catalog', axis=1).to_markdown(index=False, tablefmt="github") }}
