# Mac

!!! tip
    Mac 手成本高 但是带来的体验是非常棒的 除非找不到你想要的软件

## 应用软件

- ### 壁纸墙纸

{{ pd_read_csv('notes/soft/data/softwares.csv').query('system == "mac"').query('catalog == "壁纸墙纸"').drop('system', axis=1).drop('catalog', axis=1).to_markdown(index=False, tablefmt="github") }}

- ### 日历时间

{{ pd_read_csv('notes/soft/data/softwares.csv').query('system == "mac"').query('catalog == "日历时间"').drop('system', axis=1).drop('catalog', axis=1).to_markdown(index=False, tablefmt="github") }}

- ### 效率时间

{{ pd_read_csv('notes/soft/data/softwares.csv').query('system == "mac"').query('catalog == "效率时间"').drop('system', axis=1).drop('catalog', axis=1).to_markdown(index=False, tablefmt="github") }}

- ### 办公软件

{{ pd_read_csv('notes/soft/data/softwares.csv').query('system == "mac"').query('catalog == "办公软件"').drop('system', axis=1).drop('catalog', axis=1).to_markdown(index=False, tablefmt="github") }}

- ### 书籍阅读

{{ pd_read_csv('notes/soft/data/softwares.csv').query('system == "mac"').query('catalog == "书籍阅读"').drop('system', axis=1).drop('catalog', axis=1).to_markdown(index=False, tablefmt="github") }}

- ### PDF相关

{{ pd_read_csv('notes/soft/data/softwares.csv').query('system == "mac"').query('catalog == "PDF相关"').drop('system', axis=1).drop('catalog', axis=1).to_markdown(index=False, tablefmt="github") }}

- ### 压缩刻录

{{ pd_read_csv('notes/soft/data/softwares.csv').query('system == "mac"').query('catalog == "压缩刻录"').drop('system', axis=1).drop('catalog', axis=1).to_markdown(index=False, tablefmt="github") }}

- ### 翻译工具

{{ pd_read_csv('notes/soft/data/softwares.csv').query('system == "mac"').query('catalog == "翻译工具"').drop('system', axis=1).drop('catalog', axis=1).to_markdown(index=False, tablefmt="github") }}

- ### 财务金融

{{ pd_read_csv('notes/soft/data/softwares.csv').query('system == "mac"').query('catalog == "财务金融"').drop('system', axis=1).drop('catalog', axis=1).to_markdown(index=False, tablefmt="github") }}

- ### 手机管理

{{ pd_read_csv('notes/soft/data/softwares.csv').query('system == "mac"').query('catalog == "手机管理"').drop('system', axis=1).drop('catalog', axis=1).to_markdown(index=False, tablefmt="github") }}
