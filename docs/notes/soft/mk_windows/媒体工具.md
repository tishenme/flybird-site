# Windows

!!! tip
    Windows 的软件非常多 分享一下自己觉得好用的

## 媒体工具

- ### 屏幕截图

{{ pd_read_csv('notes/soft/data/softwares.csv').query('system == "win"').query('catalog == "屏幕截图"').drop('system', axis=1).drop('catalog', axis=1).to_markdown(index=False, tablefmt="github") }}

- ### 屏幕录像

{{ pd_read_csv('notes/soft/data/softwares.csv').query('system == "win"').query('catalog == "屏幕录像"').drop('system', axis=1).drop('catalog', axis=1).to_markdown(index=False, tablefmt="github") }}

- ### 音乐播放

{{ pd_read_csv('notes/soft/data/softwares.csv').query('system == "win"').query('catalog == "音乐播放"').drop('system', axis=1).drop('catalog', axis=1).to_markdown(index=False, tablefmt="github") }}

- ### 音频编辑

{{ pd_read_csv('notes/soft/data/softwares.csv').query('system == "win"').query('catalog == "音频编辑"').drop('system', axis=1).drop('catalog', axis=1).to_markdown(index=False, tablefmt="github") }}

- ### 视频播放

{{ pd_read_csv('notes/soft/data/softwares.csv').query('system == "win"').query('catalog == "视频播放"').drop('system', axis=1).drop('catalog', axis=1).to_markdown(index=False, tablefmt="github") }}

- ### 视频编辑

{{ pd_read_csv('notes/soft/data/softwares.csv').query('system == "win"').query('catalog == "视频编辑"').drop('system', axis=1).drop('catalog', axis=1).to_markdown(index=False, tablefmt="github") }}
