# Windows

!!! tip
    Windows 的软件非常多 分享一下自己觉得好用的

## 网络工具

- ### 浏览器

{{ pd_read_csv('notes/soft/data/softwares.csv').query('system == "win"').query('catalog == "浏览器"').drop('system', axis=1).drop('catalog', axis=1).to_markdown(index=False, tablefmt="github") }}

- ### 下载工具

{{ pd_read_csv('notes/soft/data/softwares.csv').query('system == "win"').query('catalog == "下载工具"').drop('system', axis=1).drop('catalog', axis=1).to_markdown(index=False, tablefmt="github") }}

- ### 网络连接

{{ pd_read_csv('notes/soft/data/softwares.csv').query('system == "win"').query('catalog == "网络连接"').drop('system', axis=1).drop('catalog', axis=1).to_markdown(index=False, tablefmt="github") }}

- ### 远程控制

{{ pd_read_csv('notes/soft/data/softwares.csv').query('system == "win"').query('catalog == "远程控制"').drop('system', axis=1).drop('catalog', axis=1).to_markdown(index=False, tablefmt="github") }}

- ### 社交聊天

{{ pd_read_csv('notes/soft/data/softwares.csv').query('system == "win"').query('catalog == "社交聊天"').drop('system', axis=1).drop('catalog', axis=1).to_markdown(index=False, tablefmt="github") }}

- ### 邮件客户端

{{ pd_read_csv('notes/soft/data/softwares.csv').query('system == "win"').query('catalog == "邮件客户端"').drop('system', axis=1).drop('catalog', axis=1).to_markdown(index=False, tablefmt="github") }}

- ### 网盘客户端

{{ pd_read_csv('notes/soft/data/softwares.csv').query('system == "win"').query('catalog == "网盘客户端"').drop('system', axis=1).drop('catalog', axis=1).to_markdown(index=False, tablefmt="github") }}
