# Windows

!!! tip
    Windows 的软件非常多 分享一下自己觉得好用的

## 图形设计

- ### 图片浏览

{{ pd_read_csv('notes/soft/data/softwares.csv').query('system == "win"').query('catalog == "图片浏览"').drop('system', axis=1).drop('catalog', axis=1).to_markdown(index=False, tablefmt="github") }}

- ### 图像处理

{{ pd_read_csv('notes/soft/data/softwares.csv').query('system == "win"').query('catalog == "图像处理"').drop('system', axis=1).drop('catalog', axis=1).to_markdown(index=False, tablefmt="github") }}

- ### 动画制作

{{ pd_read_csv('notes/soft/data/softwares.csv').query('system == "win"').query('catalog == "动画制作"').drop('system', axis=1).drop('catalog', axis=1).to_markdown(index=False, tablefmt="github") }}

- ### 三维动画

{{ pd_read_csv('notes/soft/data/softwares.csv').query('system == "win"').query('catalog == "三维动画"').drop('system', axis=1).drop('catalog', axis=1).to_markdown(index=False, tablefmt="github") }}

- ### 交互工具

{{ pd_read_csv('notes/soft/data/softwares.csv').query('system == "win"').query('catalog == "交互工具"').drop('system', axis=1).drop('catalog', axis=1).to_markdown(index=False, tablefmt="github") }}

- ### 设计辅助

{{ pd_read_csv('notes/soft/data/softwares.csv').query('system == "win"').query('catalog == "设计辅助"').drop('system', axis=1).drop('catalog', axis=1).to_markdown(index=False, tablefmt="github") }}
