# Windows

!!! tip
    Windows 的软件非常多 分享一下自己觉得好用的

## 系统工具

- ### 运行环境

{{ pd_read_csv('notes/soft/data/softwares.csv').query('system == "win"').query('catalog == "运行环境"').drop('system', axis=1).drop('catalog', axis=1).to_markdown(index=False, tablefmt="github") }}

- ### 系统优化

{{ pd_read_csv('notes/soft/data/softwares.csv').query('system == "win"').query('catalog == "系统优化"').drop('system', axis=1).drop('catalog', axis=1).to_markdown(index=False, tablefmt="github") }}

- ### 安全防护

{{ pd_read_csv('notes/soft/data/softwares.csv').query('system == "win"').query('catalog == "安全防护"').drop('system', axis=1).drop('catalog', axis=1).to_markdown(index=False, tablefmt="github") }}

- ### 数据恢复

{{ pd_read_csv('notes/soft/data/softwares.csv').query('system == "win"').query('catalog == "数据恢复"').drop('system', axis=1).drop('catalog', axis=1).to_markdown(index=False, tablefmt="github") }}

- ### 硬件工具

{{ pd_read_csv('notes/soft/data/softwares.csv').query('system == "win"').query('catalog == "硬件工具"').drop('system', axis=1).drop('catalog', axis=1).to_markdown(index=False, tablefmt="github") }}

- ### 窗口控制

{{ pd_read_csv('notes/soft/data/softwares.csv').query('system == "win"').query('catalog == "窗口控制"').drop('system', axis=1).drop('catalog', axis=1).to_markdown(index=False, tablefmt="github") }}

- ### 文件管理

{{ pd_read_csv('notes/soft/data/softwares.csv').query('system == "win"').query('catalog == "文件管理"').drop('system', axis=1).drop('catalog', axis=1).to_markdown(index=False, tablefmt="github") }}
